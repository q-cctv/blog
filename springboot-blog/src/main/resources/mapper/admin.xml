<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springbootblog.mapper.AdminMapper">
    <insert id="register">
        insert into admin (username, password, phone, address, email)
        values (#{username},#{password},#{phone},#{address},#{email})
    </insert>
    <select id="login" resultType="com.example.springbootblog.entity.Admin">
        select * from admin where username=#{username} and password=#{password}
    </select>
</mapper>